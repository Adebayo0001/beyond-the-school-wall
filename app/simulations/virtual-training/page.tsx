import VirtualSimulation from '@/components/pages/VirtualSimulation';

export const metadata = {
  title: 'Virtual Simulation Training Games | Beyond the School Wall',
  description: 'Live multiplayer decision labs and strategic scenario simulations where students build high-stakes situational awareness.',
};

export default function VirtualTrainingPage() {
  return <VirtualSimulation />;
}
